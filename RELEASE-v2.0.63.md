# 映迹 Yingji v2.0.63 更新说明

## 新增
- **榜单渠道大幅扩展**：内置 TMDB 模块由 16 组增至 47 组，新增「今日热门电视剧/电影、热门国产剧集/电影/综艺/动漫、热门番剧、热门台剧、爱奇艺/腾讯/哔哩哔哩/优酷/芒果TV 平台榜」等分类与平台维度。
- **IMDb / 豆瓣 外部榜单**：主进程抓取并转换为 TMDB 条目，持久化到 `yingji.discovery-cache`；面板新增「渠道筛选」tab（全部 / TMDB / IMDb / 豆瓣）+ 重试按钮，启动自动加载。
- **外壳极简模式**：透明侧边栏、海报铺满全视口为背景（`background-attachment: fixed`），所有图标浮于海报之上，去掉底栏与标题栏文字。

## 优化
- **详情页**：进入/返回多层修复（导航栈、`home()` 空数组兜底、返回按钮 `data-yj-back` 委托、CSS 避开标题栏拖拽区）；默认按已添加服务器搜索并匹配资源（`tmdb.{id}` 精确匹配）；详情信息缓存 7 天避免重复获取。
- **继续观看**：列表返回按钮内置、进度百分比移右下角长条、wide-rail 改 grid 无滚动条、全局隐藏滚动条。
- **海报**：固定 2:3 比例、横滑排布、按窗口大小决定显示数量；去排名数字、加集数徽章（S1E03 / 全8集）；去 focus 蓝框；已添加服务器获取并展示图标（Apple TV 风格）。
- **深浅色**：`system` 模式正确解析为 light/dark；浅色模式重定义 CSS 变量并覆盖硬编码组件；首页左右箭头与详情返回按钮按深浅色显示；防闪烁（面板独立渲染、hero 局部更新、`yj-no-anim`）。
- **hover**：悬浮改为 `translateY(-3px)`，不再出现遮挡海报的长方形边框。

## 技术
- 修复详情页偶发退出失败；资源匹配优先服务器来源。

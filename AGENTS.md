# Mova 开发协作约定

本文件是 AI 编程工具、自动化代理和新加入开发者进入仓库时的首要说明。开始修改前必须先阅读：

1. `docs/DEVELOPMENT_GUIDE.md`：工程结构、实现约束、验证和交付流程。
2. `DESIGN.md`：界面、交互和真实数据约束。
3. `RELEASE.md`：版本、双端构建、签名和发布流程；仅在涉及版本或发布时执行。
4. 涉及范围较大、跨模块、数据格式变化或用户行为变化的工作，先按 `docs/CHANGE_SPEC_TEMPLATE.md` 建立规格文档。

## 必须遵守

- Mova 是 Windows 与 Android 共用 `lib/` 业务代码的 Flutter 应用；改共享代码必须考虑并验证两端。
- 生产界面不得加入虚假影片、服务器、下载任务或观看进度。无数据时显示可信空态、来源状态或恢复操作。
- 延续现有视觉组件和令牌，优先复用 `lib/src/brand.dart` 与 `lib/src/motion.dart`，不要在页面内另建一套风格。
- 凭据、令牌、密码和签名文件不得提交；不得在日志、测试夹具或截图中泄露。
- 不随意改动现有持久化键、缓存格式或服务端协议。确需修改时必须说明兼容/迁移策略并添加测试。
- 不把版本号硬编码到新位置。应用版本以 `pubspec.yaml` 和 `lib/src/version.dart` 为准，发布规则见 `RELEASE.md`。
- 修改应聚焦当前需求，不顺带重构无关代码，不覆盖工作区中不属于本任务的改动。

## 完成标准

- 代码已格式化，并执行与改动匹配的静态检查和测试。
- 新功能或修复包含相应测试；若无法自动测试，记录双端人工验证步骤。
- UI 改动检查宽屏、窄屏、键盘/鼠标与触控差异；平台专属改动至少验证对应平台。
- 交付说明列出变更、验证结果、已知风险及未执行的检查。
- 未经明确要求，不提交、不推送、不打 tag、不发布。


# 映迹界面系统

## 产品界面

映迹采用重新建立的私人影音资料库界面，不继承旧版页面布局。Windows 版本以 Forward 的来源组织、Infuse 的媒体规格可读性和 Apple 平台的空间层级为基准。

## 结构

- 桌面端：系统标题栏、常驻资料库侧栏、内容工作区。
- 窄屏：同一导航组件折叠为底部标签栏。
- 首页：真实内容背景、继续观看、下一集、最近入库。
- 发现：全局搜索、最佳匹配和真实 TMDB 结果。
- 追剧：日期导航、Trakt 状态、在看与待看。
- 资料库：已连接来源、来源添加、统一媒体结果和文件播放。
- 详情：作品信息、选集、实际可用资源和 mpv 播放。
- 设置：元数据、追剧账户、来源和播放器偏好。

## 材质与排版

- 画布 `#090A0E`，结构玻璃 `rgba(36,37,46,.72)`。
- 系统字体优先，显示标题使用紧凑字距和高对比比例。
- 大面板 18–20px 圆角；普通控件 12px；状态使用胶囊。
- 玻璃只表达侧栏、浮层和任务分组，不作为装饰背景重复堆叠。

## 交互

- 按下即时缩放反馈，内容卡片使用短距离抬升。
- 页面切换保持导航位置稳定，所有入口使用真实 DOM 控件。
- 键盘方向键、Enter、Escape 和 `/` 搜索快捷键可用。
- 支持降低动态效果、降低透明度和增强对比度偏好。

## 数据约束

生产界面不得生成演示影视、虚假服务器、伪造下载任务或虚假观看进度。无数据必须展示来源状态、连接动作或错误恢复。

# 映迹界面系统

## 产品界面

映迹采用沉浸式私人影音资料库界面。Windows 版本以 WWPlayer 的桌面媒体中心结构为页面基准，并保留映迹自己的名称、图标、真实数据与功能。

## 结构

- 桌面端：全屏影视画面、左侧圆形导航、右上搜索与窗口按钮、直接叠在背景上的内容工作区。导航顺序为：首页；分割点；追剧、片单；分割点；服务器；底部固定设置。关于映迹位于设置页最底部。
- 窄屏：同一导航组件折叠为底部标签栏。
- 首页：一屏电影感画布（真实 TMDB 画面、继续观看、本周趋势与正在热播），向下滚动直接接上发现栏目，二者同属一个页面。首要操作进入资源选择，不伪装为直接播放。
- 发现（首页下半部分）：按内容栏目组织真实 TMDB 结果，并支持自定义栏目顺序，不再占用独立导航位。
- 搜索：独立搜索入口、键盘提交、错误恢复和真实 TMDB 结果。
- 追剧：九日日期导航、Trakt 状态、播出事件和本机播放记录。
- 服务器：Emby、Jellyfin 与 WebDAV 的来源添加、连接检查、媒体库浏览和文件播放。
- 片单：本地片单创建、排序、删除、详情空态和内容管理。
- 详情：作品信息、待看与片单动作、来源匹配、实际可用资源和 libmpv 播放。
- 设置：左侧分组导航，覆盖播放器、播放行为、网络与同步、字幕弹幕、缓存与维护。

## 材质与排版

- 画布 `#080A0E`；内容卡与设置工作区使用 `#1B1D22` 的高不透明石墨材质。
- 全平台统一使用内置圆润无衬线字体，不提供全局字体切换；主视觉标题 52–76px、紧凑字距和高对比字重，栏目标题 26px。
- 内容卡 14–16px 圆角；任务面板 14px；小操作和状态使用胶囊或圆形控件。
- 海报和剧照覆盖主画布；材质只用于导航、输入、资源、设置和播放器控制，不建立页面边框。
- 选中导航和主要操作使用白底深色图标，非选中导航使用近黑圆形底与白色线性图标。

## 交互

- 冷启动先显示轻量 Mova 品牌动画，完成本地设置初始化后淡入真实首页；不得使用虚假百分比，不得让启动动画阻塞失败恢复。
- 内容卡片保持位置稳定，悬停只改变亮度、阴影或边框，不发生布局位移。
- 页面切换保持导航与窗口控件位置稳定；推入式详情页统一使用固定顶部的返回、拖动区域与原生窗口按钮。
- 常规按钮沿用 Flutter 键盘焦点与 Enter 激活；Escape 退出搜索或播放器面板，`/` 打开搜索，播放器左右方向键跳转 10 秒、空格暂停或继续。
- 代理设置以真实的已连接服务器卡片呈现图标和名称；未选中表示直连，选中表示跟随系统代理，再次点击取消选中。

## 播放器

- 全屏视频是视觉主体；顶部承载返回、标题、季集、网络状态、播放设置与窗口控制。
- 底部依次显示实际媒体规格、进度条以及彼此分离的传输与功能玻璃控制岛。
- 进度条使用三层真实状态：高亮表示已播放、次高亮表示已缓冲、暗色表示尚未加载；缓存终点不得落后于当前播放位置，完整本地缓存显示到片尾。
- 音轨、字幕、画面、弹幕、章节和诊断设置从右侧控制岛向上展开，不覆盖整块视频高度。
- 选集使用宽而浅的横向圆角缩略图条，保持视频画面的连续性。
- 播放器统一使用线性图标和清晰的焦点状态。面板打开时绑定方向键与 Enter 键导航，关闭时解除绑定。
- 控件在各自结构区域内稳定排布，不遮挡其他控件，也不因悬停、焦点或弹层状态移动布局。
- 顶部显示媒体标题；存在季集编号时独立分级排列，长标题单行省略以保护窗口控制区。
- 资源、音轨、字幕与弹幕面板只展示当前 libmpv 和实际来源返回的数据，切换后同步刷新。
- Windows 点击播放后直接启动安装目录内的 `MovaNativePlayer.exe`，不再进入或嵌入 Flutter 播放页；视频和自绘控件由同一个 gpu-next/libplacebo 原生窗口承载。Android 继续使用 Flutter 播放页。
- Windows 原生播放器通过 Mova 的本机 Range 代理读取鉴权资源并复用持久缓存，退出时把实际位置和时长写回本地观看记录。
- 片头片尾规则支持播放器内手动标记、HTTP 社区来源优先级与自动跳过；面板必须注明规则来源。

## 数据约束

生产界面不得生成演示影视、虚假服务器、伪造下载任务或虚假观看进度。无数据必须展示来源状态、连接动作或错误恢复。
